{
  "name": "job-search-tracker",
  "install": "echo 'Static app — no dependencies to install'",
  "ports": [
    {
      "name": "dev-server",
      "port": 8000
    }
  ],
  "terminals": [
    {
      "name": "dev-server",
      "command": "python3 -m http.server 8000",
      "description": "Serve index.html over HTTP for IndexedDB and bundled fonts"
    }
  ]
}
